export interface SupportedComponentsModel {
  songDetails: boolean,
  songStatus: boolean,
  playerHealth: boolean,
  score: boolean,
  highScore: boolean,
  combo: boolean,
  multiplier: boolean,
  gameSpecific?: boolean,
}
